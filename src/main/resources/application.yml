spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lll
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG
      hibernate:
        sql: DEBUG